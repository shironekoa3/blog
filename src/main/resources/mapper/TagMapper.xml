<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.javac.blog.mapper.TagMapper">

    <select id="listAll" resultType="me.javac.blog.entity.Tag">
        SELECT *, (SELECT COUNT(1) FROM article_tag WHERE tag_id = tag.`id`) AS `ref_count`
        FROM tag
        WHERE is_deleted = 0
    </select>


</mapper>
