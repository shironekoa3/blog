<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.javac.blog.mapper.CategoryMapper">

    <select id="listAll" resultType="me.javac.blog.entity.Category">
        SELECT *, (SELECT COUNT(1) FROM `article` WHERE `article`.`category_id` = `category`.`id`) AS `article_count`
        FROM `category`
        WHERE is_deleted = 0
    </select>

</mapper>
